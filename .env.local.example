# Point the frontend at the local FastAPI backend during development.
# Start infrastructure: docker compose up -d
# Start API: cd backend && uvicorn main:app --reload --port 8001
VITE_API_BASE=http://localhost:8001/api
